Runs correctly in windows 10, should run on any version of windows or any system where 1 byte 
is 8 bits because I utilize int8_t to handle memory sizes accuratly. 

A functioning virtual MIPS pipelined processor with branch prediction and forwarding.
Implements MIPS assembly. Not all commands are implemented but additional commands can be easily added.
